import styled from "styled-components";

const InfoPopUpStyleWrapper = styled.div`
  // 스타일 입력
  .loading {
    display: flex;
    justify-content: center;
  }

  .toc-content {
    width: 100%;
    height: 500px;
    overflow-y: auto;
    resize: none;
  }
`;

export default InfoPopUpStyleWrapper;
